[install]
# Bun resolves to lockfile entries by default. Set exact=true if we ever want
# semver pinning enforced for new adds.
exact = false

[test]
coverage = true
coverageReporter = ["text"]
coverageSkipTestFiles = true
coveragePathIgnorePatterns = ["src/cli.ts", "src/plans-cli.ts", "src/skill-cli.ts", "src/*-runtime.ts", "agent/skills/impeccable/**", "tests/setup.ts"]
coverageThreshold = 0.8
preload = ["./tests/setup.ts"]
